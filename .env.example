# Copy this file to .env and update values.

# Application security
ITAM_SECRET_KEY=replace-with-a-long-random-secret

# SQL Server connection string (example: local SQLEXPRESS)
ITAM_DB_URL=mssql+pyodbc://apitam_user:StrongPassword123!@localhost\\SQLEXPRESS/itam_db?driver=ODBC+Driver+17+for+SQL+Server

# Optional: override Uvicorn host/port in your startup scripts if needed
ITAM_HOST=127.0.0.1
ITAM_PORT=8000

# Optional: public URL used in your deployment documents and reverse proxy setup
# Example: https://itam.yourcompany.com
ITAM_PUBLIC_URL=http://127.0.0.1:8000
